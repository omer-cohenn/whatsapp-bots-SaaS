# STATUS — read this first to resume 📍

> **Last updated: 2026-07-20.** This is the single "where are we / how do I continue" file. A new session
> should read this, then [`spec/mvp-checklist.md`](spec/mvp-checklist.md).
> **Getting onto the server:** [`SERVER.md`](SERVER.md) — access, file map, how it runs, troubleshooting.

## 🟢 LIVE IN PRODUCTION — https://botik-dev.duckdns.org (since 2026-07-17)
AWS Lightsail (Frankfurt, Ubuntu 24.04, 1GB + 2GB swap) · static IP `35.157.230.101` · free DuckDNS
domain · **Caddy** terminates auto-HTTPS and is the ONLY public door. Deploy runbook:
[`DEPLOY.md`](DEPLOY.md) · decision: [`decisions/0025-production-deployment.md`](decisions/0025-production-deployment.md).

**Latest shipped: M17** — leads keyword search + Excel export (one tab per flow), commit `74d6bcc`,
live 2026-07-20. Decision: [`decisions/0027-m17-leads-search-and-export.md`](decisions/0027-m17-leads-search-and-export.md).
> ⚠️ The search match rule exists TWICE — `backend/app/services/leads/search.py` and
> `frontend/src/dashboard/leadSearch.ts`. They MUST stay identical, or the exported file stops
> matching the on-screen list. Change both in the same commit.

### Open items (honest list)
1. **GitHub Actions auto-deploy is FAILING** on the `DEPLOY_SSH_KEY` secret — manual
   `sudo bash /root/deploy.sh` on the server is the working path until it's fixed.
   (Log in as `ubuntu`, NOT `root` — see [`SERVER.md`](SERVER.md).)
2. **Rotate `GOOGLE_CLIENT_SECRET` + `GEMINI_API_KEY`** — flagged in
   [`security/hardening-report.md`](security/hardening-report.md), still not done.
3. **Buy a real domain** later; DuckDNS is the free interim (changing it = `PUBLIC_DOMAIN` in
   `infra/.env` + the Google OAuth redirect URIs).
4. **Plans-catalog test drift** — 5 admin tests still expect the removed `basic` plan (migration
   `0022` replaced the catalog with חינמי/מקצועי/עסקי).
5. **23 known pre-existing test failures** from the greeting commit (`0dd58ad`, M8/M9/bot_sim/
   dashboard/lead_status) — not a regression, the old assertions need updating.
6. **Consider adding the Postman/newman gate** into the deploy workflow (collection already exists
   in `tests/postman/`, 18/18 green when run by hand).
7. **Server memory is tight, but NOT because of bot count** — the baseline is infrastructure:
   `dockerd` alone is 123MB, `containerd` 28MB, before any app code. Bot count barely moves it.
   `available` is ~180MB. The `reverse-proxy` *healthcheck* has been failing for 3+ days with
   `fork: Resource temporarily unavailable` (the site itself still serves 200). Lever if headroom
   is ever needed: `WEB_CONCURRENCY` is 2 — dropping to 1 frees ~30MB. Measured table in
   [`SERVER.md`](SERVER.md) §5.
8. ~~`infra/.env.bak.<ts>` on the server~~ — **DONE 2026-07-20.** Shredded after verifying nothing
   referenced it and that `.env` held all 27 keys; `.env` perms tightened 644 → 600. Next-deploy
   safety re-verified with `docker compose config -q`.
9. **Demo seed data in the admin business** — an insurance bot ("שקד סוכנות לביטוח", 4 flows) plus
   300 generated leads and 8 tiny real files in R2, all tagged `cache_chat_ref LIKE 'seed:demo300:%'`
   for a one-predicate cleanup. The previous barbershop bot config was backed up before it was
   replaced. This is LOCAL-dev data; it is not on the production server.

## Phase: BUILD (the MVP). Mapping + ground-up re-spec are DONE.
- ✅ **M0** — the stack runs (one command, all services healthy).
- ✅ **M1** — WhatsApp receive works **end-to-end** (real inbound messages reach the backend webhook).
- ✅ **M2** — the tenant wall: 9 tables + 2 non-service DB roles + RLS (`USING`+`WITH CHECK`) + dual-key fail-loud encryption + Redis cache isolation + the isolation suite. Migrations auto-apply via the compose `migrate` step. **Verify:** `make demo-isolation` (9/9), `make isolation` (10 passing), `make demo-break` (8/9 then restored).
- ✅ **M3** — login & accounts: Google OAuth + opaque Redis-backed sessions (`bizzup_session`) + the **deny-by-default** `/api` gate, wired onto the M2 tenant session. `provision_owner` auto-creates a business on first login (idempotent); logout truly destroys the session. **Verify:** `tests/test_m3.bat` → M3 narrated **5/5**, the `test_auth_gate.py` pytest gate green, and the M2 wall still **12/12** (no regression). A real Google click-through is a one-time manual check at `:5173`.
- ✅ **M4** — the AI bot builder (per-tenant config, validated/grounded). **Verify:** `tests/test_m4.bat` → M4 narrated **9/9**.
- ✅ **M5** — the bot brain + leads: the **pure conversation engine** + the **lead-memory runtime** (`bot_runtime.run_turn` + `/api/bot/sim`, is_test) + **human-handoff silence** + the **abandoned sweep** (migration 0006 `sweep_abandoned_leads`). Leads walk in_progress→new/abandoned with started/step/completed/abandoned funnel events; **PII (phone/contact_name/answers) encrypted at rest — asserted on the raw columns**. **Verify:** `tests/test_m5.bat` (try-me **18/18**) + `tests/test_m5b.bat` (lead-memory narrated **10/10**, strict `test_bot_sim.py` **6/6**), with M2 **12/12** + M3 + M4 + try-me all still green. (try-me chat UI is frontend, not part of this backend verification.)
- ✅ **M7** — the owner dashboard (back-office): `GET /api/leads` (decrypted phone/name/ALL answers; period/status incl. synthetic 'open'/flow filters; is_test excluded by default), `GET /api/dashboard` (funnel started/completed/abandoned/total, matches DB truth), `GET /api/conversations` + `POST .../status` (bot/human/closed) + `POST .../reply` (queued to outbox), `PUT /api/bot/publish` (reflected by GET /api/bot/settings). All six session-gated (401) + tenant-isolated (A never sees B). **Verify:** `tests/test_m7.bat`/`test_m7b.bat` → M7 narrated **15/15**, strict `test_dashboard.py` green (the `period=week|month` interval bug is **FIXED** — `leads._PERIOD_INTERVALS` now uses `datetime.timedelta`), frontend `tsc --noEmit` clean, full strict bundle **91 passed**, and M2–M5 all still green. **M7 polish:** owner-settable lead statuses `deal` (בוצעה עסקה) / `closed` (ליד סגור) via `PATCH /api/leads/{id}/status`; an `orders` KPI (= deals) on `GET /api/dashboard`; the home shows a recent-activity feed (replaced system-health); each lead has a WhatsApp (`wa.me`) button + manual status buttons.
- ✅ **M8** — the in-app human-handoff chat: a full Redis transcript (`conv:{business}:{conv}:log`, roles customer/bot/owner, LTRIM-200, tenant-isolated), the new `waiting` status (customer asked for a human, nobody picked up yet), the **bot is SILENT in `waiting` AND `human`** (but the inbound customer message is STILL logged), handoff sets `waiting` + logs the `handed_off` funnel event, and the read surface `GET /api/conversations/{id}` (status + linked decrypted lead + messages) · `GET .../messages` · `POST .../reply` (now also appends role=owner) · `POST .../status` (Literal widened to `waiting`). **Verify:** `tests/test_m8.bat` → M8 narrated **27/27** + strict `test_m8.py` **17/17**; the strict M2–M8 bundle **108 passed**; M2 **12/12**, M3 **5/5**, M4 **9/9**, M5 **18/18** + M5b **10/10**, M7 **15/15** all still green. Note: the M5 handoff tests were updated to the M8-locked contract (handoff → `waiting`, not `human`) — product source already implemented `waiting`; only the stale test assertions changed.
- ✅ **M9 (lead outcomes)** — outcomes unified around the LEAD (decision 0009): the lead status is the single source of truth. "בוצעה עסקה" → lead `deal` + chat `closed`; "סגירת פנייה" → lead `closed` + chat `closed`, both via the EXISTING `PATCH /api/leads/{id}/status` + `POST /api/conversations/{id}/status` (no new endpoint). `GET /api/leads?status=` now accepts `deal`/`closed`; every lead carries `conversation_id` (derived from `cache_chat_ref`, null when absent); **every handoff ALWAYS yields a lead** — a handoff with no prior lead creates a minimal `"פנייה לנציג"` lead (status `in_progress`), links it to the chat, and attaches the `handed_off` funnel event (status still flips to `waiting`). **Verify:** `tests/test_m9.bat` → M9 narrated **11/11** (incl. a negative control) + strict `tests/test_m9.py` **10/10**; the strict M3–M9 bundle **118 passed**; M2 **12/12**, M3 **5/5**, M4 **9/9**, M5 **18/18** + M5b **10/10**, M7 **15/15**, M8 **27/27** all still green. No M8 handoff assertions needed changing — none had asserted "handoff → no lead".
- ✅ **M10 (transcript TTL by status + private outcome note)** — decision 0010, two promises. (1) **The live transcript no longer vanishes while a human is needed:** TTL is centralized in `conversation_state._apply_ttl` and applied by BOTH `set_status` AND `append_message` using the CURRENT status — `bot` slides ~60 min, `waiting`/`human` **PERSIST** (no expiry), `closed` = 30 days; covers the conv hash key, its `:log`, and the index. `append_message` STOPPED unconditionally re-setting the 60-min TTL (the bug that silently deleted a waiting chat on the next message). (2) **Encrypted outcome note:** migration `0007_outcome_note.sql` (additive/idempotent) adds `leads.outcome_note`; `PATCH /api/leads/{id}/status` accepts `{status, note?}`, encrypts the note at rest (raw column is ciphertext, asserted), and `LeadItem` returns it decrypted for the owner (null when unset) — tenant-scoped (A can't set/read B's). **Verify:** `tests/test_m10.bat` → M10 narrated **10/10** (incl. a negative control) + strict `tests/test_m10.py` **13/13**; the strict M3–M10 bundle **131 passed**; M2 **12/12**, M3 **5/5**, M4 **9/9**, M5 **18/18** + M5b **10/10**, M7 **15/15**, M8 **27/27**, M9 **11/11** all still green. No old assertions needed changing — product source already implemented the locked M10 contract.
- ✅ **M11 (public booking + per-business Google Calendar)** — decision 0011. A public, non-guessable booking page (slug→tenant via the `resolve_booking_slug` SECURITY DEFINER + a normal RLS-scoped `tenant_connection`, mirroring the login bootstrap), backed by four new tables (migrations `0008_booking` + `0009_rls_booking` + `0010_booking_slug_resolve`, all additive/idempotent, RLS ENABLE+FORCE on all four, `app_role` full CRUD / `gateway_role` nothing). **Slot algorithm:** SPLIT working-hours ranges per weekday (e.g. 09–13 + 16–19), per-service durations, and the rules `min_notice_minutes` / `buffer_minutes` / `max_days_ahead` all computed in **Asia/Jerusalem** and stored **UTC** (zoneinfo). **Public booking** creates a unified **LEAD + booking** in one transaction with all client PII (name/phone/email/notes) **encrypted at rest — asserted on the raw columns** + decrypted only for the owner; guards: double-booking → **409**, unknown slug → **404**, junk fields → **422**, off-grid time rejected. **Cancel/reschedule** via the customer's non-guessable `cancel_token` AND via owner `PATCH /api/bookings/{id}`. **Google Calendar OPTIONAL** per business via a decoupled hook (mock-tested): the create-event hook fires with the right params (attendee email + Asia/Jerusalem timeZone), **Meet only when `meet_enabled`**, a Google failure **degrades gracefully** (booking still 201), and the KEK-encrypted **refresh_token never appears in any response**. **Verify:** `tests/test_m11.bat` → M11 narrated **21/21** (incl. an active negative control that drops RLS on `bookings`, catches the cross-tenant leak, then restores) + strict `tests/test_m11.py` **28/28**; the strict M3–M10 bundle **131 passed**; M2 **12/12** + M3 **5/5** + M4 **9/9** + M5 **18/18** + M5b **10/10** + M7 **15/15** + M8 **27/27** + M9 **11/11** + M10 **10/10** all still green. **C4 re-proved:** A cannot read/list/PATCH B's bookings/services/settings (foreign PATCH → 404); admin routes 401 without a session; the public slug only ever exposes its own tenant. (Booking-reminder sends ride on M6 WhatsApp — for now they queue to a PII-free Redis outbox.)
- ✅ **M11.1 (public booking page polish)** — decision 0012. Richer service cards + a per-business welcome message + a day-availability call for the public calendar; **NO images / no file storage**. **Migration `0011_booking_service_extras`** (additive/idempotent): `services.description`, `services.price` (nullable, ≥0 ₪, app-enforced), `booking_settings.welcome_message` — no new RLS/grant (existing `services`/`booking_settings` policies cover new columns). **Service cards:** description (≤500) + OPTIONAL price flow through create/update/list/get + the public read; PATCH distinguishes *omit* (untouched) from *explicit null* (clear) via `model_fields_set`; price `0` is a valid free price, empty stays null (UI shows "ללא עלות"). **Welcome message:** `welcome_message` (≤600) persists on `PUT /api/booking/settings` + returns on GET, and rides the public `GET /api/book/{slug}/services` response (None when unset). **Availability:** new public `GET /api/book/{slug}/availability?service_id=&from=&to=` → `{dates:[...]}` (days with ≥1 free slot, loops `compute_slots`); range bounded ≤62 days (else **422**), inverted/bad → **422**, unknown slug → **404**. **AI welcome:** new gated `POST /api/booking/welcome/generate` (`current_business`) — dedicated Hebrew prompt in `booking_welcome` mirroring `bot_builder_ai` (gemini-3.1-flash-lite, validate-at-use → **503** no key / **502** call fail), never logs/leaks the key. **Verify:** `tests/test_m11_1.bat` → M11.1 narrated **20/20** (incl. an active negative control: drop RLS on `booking_settings` → catch the welcome-message leak → restore) + strict `tests/test_m11_1.py` **20/20**; the strict M3–M11 bundle **179 passed** (159 prior + 20 new); M2 **12/12** + M11 **28/28** all still green. No product source was edited to pass — the locked M11.1 contract was already implemented; the only fix was a test-helper that read back the server-owned slug after an upsert.
- ✅ **M11.2 (service card image)** — each service can carry a PICTURE with **no external file storage**: the owner uploads a photo and the frontend **resizes it client-side (canvas)** into a small compressed `data:` URL stored in the new `services.image_url` column (a plain http(s) URL is also accepted in the same field; NULL => the public card shows a placeholder frame). **Migration `0012_service_image`** (additive/idempotent): `ALTER TABLE services ADD COLUMN IF NOT EXISTS image_url text` — no new RLS/grant (existing `services` policies cover the new column). **Backend:** `ServiceItem`/`ServiceCreateRequest`/`ServiceUpdateRequest`/`PublicServiceItem` gain `image_url` (Pydantic `max_length=2_000_000` so a ~1 MB resized data URL fits); `booking.py` create/update/list/get/`_service_to_dict` carry it through SELECT/INSERT/UPDATE/RETURNING; PATCH distinguishes *omit* (untouched) from *explicit null* (clear) via `model_fields_set`/`set_image_url`; the public `GET /api/book/{slug}/services` includes it. **Frontend:** `BookingFlow.tsx` service card matches the approved prototype — a top image frame (`rounded-2xl`, `aspect-[16/9]`, soft blue-tinted border) showing the photo (`object-cover`) or a centered `מקום לתמונה` placeholder; title/description below, a duration chip + price row, selected = blue ring (#2563EB) + check badge; owner upload + canvas resize in `ServicesEditor.tsx` + `lib/imageResize.ts`. **Verify:** `tests/test_m11_2.bat` → M11.2 narrated **13/13** (incl. a **non-destructive negative control**: A's own DB query for B's image returns NULL while B sees its own — RLS proven without weakening it) + strict `tests/test_m11_2.py` **11/11**; the strict M3–M11.2 bundle **190 passed** (179 prior + 11 new); M2 **12/12** + M11 **28/28** + M11.1 **20/20** all still green. No product source was edited to pass — the locked M11.2 contract was already fully implemented. (Pre-existing, unrelated: standalone `tsc --noEmit` flags missing `date-fns`/`react-day-picker` in the container's stale node_modules for the M11 `BookingsCalendar`/`DatePicker` components — declared in `package.json`, not an M11.2 file; M11.2-touched files typecheck clean.)
- ✅ **M6a ("Message Yourself" self-test slice — decision 0014)** — QA-verified 2026-06-22. The owner rehearses their LIVE bot by messaging their OWN number: the gateway flags the self-chat (`self_test:true` + `conversation_id`), POSTs `/webhook/whatsapp`, the backend resolves `gateway_account_id → business_id` SERVER-SIDE (the `resolve_wa_account` SECURITY DEFINER), **gates on `is_published`**, runs the FULL bot (`bot_runtime.run_turn`, `is_test=False` → REAL leads/funnel/booking-link/handoff), and returns `{status, replies}` for the gateway to type back into the self-chat. Admin routes `GET/POST /api/whatsapp/{status,link,qr}` (session-gated, tenant-scoped). Gateway loop-guard (sent-id Set, cap 200, oldest-evicted) prevents reply-to-self. **Verify:** `tests/test_m6a.bat` → M6a narrated **10/10** (incl. an active negative control: un-publish → silent → re-publish → answers) + strict `tests/test_m6a.py` **12/12**; the strict M3–M11.2 + M6a bundle **202 passed**; M2 **12/12** (no isolation regression). **QA integration fix:** `backend/app/api/me.py` imported `whatsapp_router` but never `include_router`'d it (the `/api/whatsapp/*` routes 404'd) → added the missing `api.include_router(whatsapp_router)`. **MANUAL for Omer:** (1) the real phone send/receive (scan QR → "Message Yourself" → bot replies); (2) recreate the long-running `backend` + `gateway` containers so they load the new `/api/whatsapp/*` + gateway `/info` routes (both 404 on the current 18–19h-old containers — no auto-reload).
- ✅ **M6a.1 (external "test numbers" allow-list — decision 0014)** — QA-verified 2026-06-22. The owner registers up to **5 OUTSIDE phone numbers** (each an optional name); when ANY of those messages the owner's linked WhatsApp, the gateway forwards it (`self_test:false`, always carrying `conversation_id`) and the backend runs the FULL bot (`run_turn`, `is_test=False` → REAL leads) and replies — **exactly like the self-chat**. Everyone NOT on the list is **silent** (`{status:"not allowed", replies:[]}`); the **publish gate still applies** (allow-listed + draft → `{status:"not published", replies:[]}`); self-chat keeps working unchanged (owner always allowed). The self-test + allow-list inbound paths share ONE `_run_bot_turn` core (resolve→published→run_turn), differing only in the allow gate. **Migration `0014_whatsapp_test_numbers.sql`** (RLS ENABLE+FORCE, `p_tenant_isolation` on `current_business_id()` mirroring `whatsapp_connections`; `app_role` SELECT/INSERT/UPDATE/DELETE; no `gateway_role`). **Phone + label encrypted at rest** (bytea ciphertext — asserted on the raw columns); `normalize()` (digits-only, strip `+`, Israeli `0XXXXXXXXX`→`972XXXXXXXXX`) is the single source of truth applied to BOTH stored numbers AND the inbound `from`, so a stored `0547408309` matches an inbound `+972547408309`. Admin `GET/PUT /api/whatsapp/test-numbers` (session-gated, tenant from session, ≤5 cap → 422, blanks/dupes dropped, labels round-trip). **Verify:** `tests/test_m6a1.bat` → M6a.1 narrated **11/11** (incl. an active negative control: drop a number → ignored → restore → answers) + strict `tests/test_m6a1.py` **16/16**; the strict M3–M11.2 + M6a + M6a.1 bundle **218 passed**; M2 **12/12** (no isolation regression). **No product source was edited to pass — the frozen M6a.1 contract was already correctly implemented across data/backend/gateway.** **MANUAL for Omer:** the real phone send/receive from a SECOND phone you own (add it to "מספרים לבדיקה", message your linked WhatsApp from it → bot replies; a phone NOT on the list gets no reply).
- ✅ **M6a.2 (owner manual reply → WhatsApp)** — QA-verified 2026-06-22. Closes the M6a hole: the owner's manual handoff reply (`POST /api/conversations/{id}/reply`) was only QUEUED in Redis — nothing sent it (only the bot's own auto-replies were sent, inline via the webhook response). Now the reply is STILL queued first (`append_reply` unchanged → outbox + transcript, never lost) and then **best-effort sent to WhatsApp** on top. The `conversation_id` IS the customer's WhatsApp jid, so it doubles as the send `to`. **New gateway door** `POST /send-bot` (internal, Docker network): `X-Gateway-Token` constant-time check (401 before any work), `{to,text}` validated (400 on missing/blank), 503 when not connected, **200 `{ok:true, message_id}`** on send — and the sent id is recorded in the loop guard (`rememberSentId`) so its `fromMe` echo never re-triggers the bot (covers both the `@s.whatsapp.net` customer and the `@lid` self-chat). **New backend helper** `whatsapp.send_outbound(to, text) -> bool` (httpx POST, 8s timeout): **True only on a 2xx + `{"ok":true}`**, graceful **False** on any failure (gateway down/timeout/non-2xx/bad shape) so the owner's request never errors; **never logs the jid/text/token**. `ConversationReplyResponse` gains `delivered: bool = False` (backward-compatible); the route returns `{conversation_id, queued:true, delivered:bool}`. **Verify:** `tests/test_m6a2.bat` → M6a.2 narrated **10/10** (incl. a **LIVE** gateway send: a real WhatsApp message to the owner's OWN number/self-chat, confirmed 200 + message_id in the gateway log with PII-safe `{textLen,hasMessageId}` only) + strict `tests/test_m6a2.py` **13/13** (the live 200 send actually ran — gateway connected); the strict M3–M11.2 + M6a + M6a.1 + M6a.2 bundle **231 passed**; M2 **12/12** (no isolation regression). **No product source was edited to pass — the frozen M6a.2 contract was already correctly implemented across backend + gateway.** **MANUAL for Omer:** from a phone, message your linked WhatsApp so a chat appears → set it to "human" → type a reply → it arrives on the customer's WhatsApp.
- ✅ **M12 (platform-operator BACK-OFFICE — decision 0016)** — QA-verified 2026-06-22. Omer's mall control room: ONE window onto EVERY business + ACTIONS, the one surface that deliberately crosses the tenant wall behind its OWN locked door. **Data (migrations 0015–0017, additive/idempotent):** `plans` (global catalog, `app_role` SELECT only) + `subscriptions` (PK=business_id, **no** direct `app_role` grant — SD-only) + `businesses.is_active` (the suspend switch); `usage_daily` (RLS **ENABLE+FORCE**, `p_tenant_isolation` on `current_business_id()`, `app_role` SELECT/INSERT/UPDATE so a tenant bumps **its own** counters) + `admin_audit` (SD-write only, **no** direct grant); five `SECURITY DEFINER` admin_* funcs (`admin_overview`/`admin_list_businesses`/`admin_business_detail`/`admin_usage_series`/`admin_set_subscription`), `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO app_role`, `search_path` pinned — the ONLY cross-tenant doorway. **Backend:** `ADMIN_EMAILS` (env, empty allowed, **not** fail-closed); `current_admin` (session + email∈ADMIN_EMAILS, checked **LIVE** → 403 else); `/api/admin/*` router gated by `current_admin`, `/api/me` gains `is_admin` (computed live); `services/usage.bump_safe` wired at the webhook (msg_in/msg_out), `leads.create_lead` (lead), `booking.create_public_booking` (booking), auth callback (login); **webhook suspend gate** (`businesses.is_active` checked alongside `is_published` → a suspended business's bot stays SILENT). **Frontend:** admin client/types, `isAdmin` in AuthContext, admin-only "ניהול" nav, `/admin` + `/admin/businesses` + `/admin/businesses/:id` pages + in-house SVG usage chart. **Verify:** `tests/test_m12.bat` → M12 narrated **13/13** (incl. an active negative control: flip `is_active=false` by hand → bot silent → restore → answers) + strict `tests/test_m12.py` **26/26** (gate: no-session 401 + non-admin 403 + admin 200 on every route; overview reconciles with DB truth; list/detail defaults free/active + search + paging bounds 422 + unknown id 404; set-subscription 422/422/404 + audit row stamped with the REAL admin Google sub+email + is_active sync; **suspend silences the webhook bot, re-activate restores**; usage bump → right metric/day + series + bad-date 422; **isolation:** app_role CANNOT directly SELECT subscriptions/admin_audit (permission denied) — SD funcs are the only path — and A can't read B's `usage_daily`; no PII/secret in any admin response); the strict M3–M11.2 + M6a/.1/.2 + M12 bundle **257 passed**; M2 **12/12** (no isolation regression). **QA regression fix (test-only, no product source):** `tests/test_auth_gate.py::test_api_me_valid_session_returns_own_business` froze the OLD `/api/me` shape `{user,business,connection}` — M12 added the contracted `is_admin` key, so the assertion now expects `{user,business,connection,is_admin}` and asserts `is_admin is False` for non-admin Avi. **No product source was edited to pass — the frozen M12 contract was already correctly implemented across data/backend/frontend.** **MANUAL for Omer:** log in with `oyc3333@gmail.com` at `:5173` → the "ניהול" tab appears → open a business → change its plan / suspend it → confirm a suspended business's bot stops replying on WhatsApp.
- ✅ **WhatsApp inbound LID→phone fix (2026-06-23, commit `dd10dea`)** — modern WhatsApp addresses 1:1 chats by a hidden `@lid` privacy id, so `msg.key.remoteJid` is a LID, not the phone → the M6a.1 external test-number allow-list NEVER matched (it compared a LID's digits to a stored phone). Fix in the gateway `contract.js`: derive `from` from `msg.key.senderPn || remoteJid` (Baileys 6.7.x exposes the sender's real phone-number jid as `senderPn`), so the backend's existing phone-normalized `is_number_allowed` matches; `conversation_id` stays the `remoteJid` (the stable reply key). No backend change. Self-chat (owner LID matched to `sock.user.id`) was unaffected and already worked. ✅ **Live-verified 2026-06-23:** an external phone on the allow-list (Omer's wife, `0508648315`) messaged the linked WhatsApp → the bot replied (the LID fix works end-to-end). Also fixed today: the admin "ניהול" nav tab now renders in a distinct blue (`#378ADD`) to stand out. (Recurring trap re-confirmed: a Baileys 428 close can wedge the socket so it stops forwarding — `docker restart` the gateway to recover, no QR rescan.)
- ✅ **M13 (back-office ANALYTICS + platform-level sales CRM — decision 0017)** — QA-verified 2026-06-23. The big extension of M12: a real SaaS-owner dashboard + a sales pipeline, all still behind M12's one locked admin door, with the tenant wall un-weakened. **Data (migrations 0018–0020, additive/idempotent):** three operator-only tables with **no** direct `app_role`/`gateway_role` grant (SD-only, exactly like `subscriptions`/`admin_audit`) — `platform_snapshots` (PK=day, one daily aggregate row for trend history), `business_crm` (PK=business_id, pipeline stage new→contacted→warming→won/lost + last/next-followup, `updated_at` trigger), `crm_notes` (per-business action log, soft FK admin_user_id→users(id)); **12 new `SECURITY DEFINER` funcs** (`admin_leads_by_type`/`admin_messages_by_business`/`admin_ai_ops_series`/`admin_by_plan`/`admin_trends_series`/`admin_business_extra`/`admin_ltv_summary`/`admin_crm_list`/`admin_set_crm_stage`/`admin_add_crm_note`/`admin_crm_notes`/`admin_upsert_today_snapshot`), each `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO app_role`, `search_path` pinned; both CRM writers stamp the REAL admin Google sub+email into `admin_audit` + `crm_notes`. LTV = plan price × tenure-months (floor 1; free→0) computed in SQL — flagged an estimate, no real billing. **Backend:** `usage.METRIC_AI_CALL` + `bump_safe` at the bot-builder AI endpoint (`api/bot_builder.py`) and the booking-welcome AI endpoint (`api/booking.py`); new `/api/admin` routes (all inherit `current_admin`): GET `analytics/leads-by-type` · `analytics/messages` · `analytics/ai-ops` · `analytics/by-plan` · `analytics/trends`, GET `crm`, PATCH `businesses/{id}/crm`, POST (201) + GET `businesses/{id}/crm/notes`; EXTENDED GET `overview` (now stamps `admin_upsert_today_snapshot` best-effort + adds `avg_ltv`/`total_ltv`) and GET `businesses/{id}` (adds `ltv_estimate`/`ai_calls`/nested `crm`). **Frontend:** admin client/types extended, in-house SVG LineChart/DonutChart/StackedBarChart, enriched AdminHome, new `/admin/crm` pipeline board + `/admin/billing` messages-per-business, BusinessDetail LTV/ai_calls/CRM panel (tsc/lint/build pass). **Verify:** `tests/test_m13.bat` → M13 narrated **16/16** (incl. an active negative control: GRANT app_role a direct SELECT on `crm_notes` → read succeeds so the test CAN catch a leak → REVOKE → denied again, restored) + strict `tests/test_m13.py` **30/30** (gate: no-session 401 + non-admin 403 + admin 200/201 on every new route; analytics reconcile to DB deltas — leads-by-type buckets booking/lead/handoff + plan filter, messages totals=in+out, by-plan grouping, ai-ops day-sum, trends after a snapshot; LTV estimate price×tenure + free→0 + summary reconciles to `admin_ltv_summary`; `ai_call` bump lands in `usage_daily` + surfaces in detail/by-plan; **snapshot idempotent — two overview views keep ONE row**; CRM stage move + REAL-admin audit, bad stage 422, unknown biz 404, note round-trip newest-first + note_count++, blank note 422; **isolation:** app_role CANNOT directly SELECT `business_crm`/`crm_notes`/`platform_snapshots` (permission denied) — SD funcs are the only path — and A can't read B's `usage_daily`; no end-customer PII in any analytics/CRM response). The strict M3–M12 + M13 bundle **287 passed**; M2 **12/12** (no isolation regression). **QA regression fix (test-only, no product source):** `tests/test_m12.py::test_admin_responses_carry_no_lead_or_booking_content` froze the OLD detail-key allow-list — M13 adds the additive aggregate/sales keys `ltv_estimate`/`ai_calls`/`crm`, so the allow-list now includes them and the test also asserts the nested `crm` block carries only `stage`/`last_contacted_at`/`next_followup_at` (no customer PII). **No product source was edited to pass — the M13 contract was already correctly implemented across data/backend/frontend.** **MANUAL for Omer:** log in with `oyc3333@gmail.com` at `:5173` → "ניהול" → see the LTV/messages/leads-by-type/AI/trends dashboard, open `/admin/crm` to drag a business across the pipeline + add a note, and `/admin/billing` for messages-per-business. Plan: [`decisions/0017-m13-backoffice-analytics-crm.md`](decisions/0017-m13-backoffice-analytics-crm.md).
- ✅ **M13 follow-ups (2026-06-23, commits `4eb0f81`/`0615eeb`/`08f88da`)** — (1) the per-business "נתוני שימוש" section now also charts **`ai_call`** ("פעולות AI") and has a **"מקלות / מצטבר" toggle** (per-day bars vs a cumulative running-total line; the line got an **area fill + date-range caption** so a flat cumulative reads clearly). (2) **Admin can permanently delete a business** — **migration `0021` `admin_delete_business`** (SD, EXECUTE app_role only; audits BEFORE the cascade `DELETE FROM businesses`, and the `admin_audit` row has no FK to businesses so it survives) + `DELETE /api/admin/businesses/{id}` (refuses the admin's OWN session business → **400**; unknown/malformed id → **404**; real admin identity into the audit) + a red **"אזור מסוכן"** card on the business page with a **type-the-name confirm Modal**. **Verified:** a DB cascade test (`biz_left=0`, `usage_left=0`, `audit_rows=1`), gate **401** without a session, frontend tsc/lint/build clean. Done inline (not via the milestone agents); the full M13 test suite was NOT re-run for these two additions.
- ✅ **M14 (two bot fixes)** — QA-verified 2026-06-23. **FIX A — the opening message no longer lists the menu twice.** The engine's `_greeting_and_menu` glues the stored `greeting` + the canonical, number-matched `_menu_list`; the AI builder had been baking its OWN option list/numbers INTO the greeting, so options appeared twice (and in a different order → a typed number could pick the wrong flow). Fix is PROMPT-only: `bot_builder_ai.build_system_prompt` now carries an explicit "greeting = short warm Hebrew intro ONLY, no option list, no numbers (the system renders the numbered menu automatically right after)" constraint + an options-free example/placeholder. **The engine is unchanged** (`_greeting_and_menu` still appends `_menu_list`). **FIX B — a CLOSED chat restarts fresh on a new inbound.** New `conversation_state.reset_conversation(redis, business_id, conversation_id)` deletes this conversation's conv hash (status + engine `state` field) + transcript `:log` + index entry, all tenant-scoped (`_assert_owns` on both per-conversation keys); after it `get_state`→None (→`initial_state`), `get_status`→None (callers default 'bot'), transcript empty. `bot_runtime.run_turn` calls it right after the waiting/human silent-check when `status == STATUS_CLOSED`, then FALLS THROUGH — so a closed chat + new message behaves EXACTLY like a brand-new conversation (fresh transcript reseeded with just the new exchange, greeting+menu open turn, a NEW lead when a flow is picked); the OLD closed lead row in Postgres is left untouched (history preserved). **Verify:** `tests/test_m14.bat` → M14 narrated **12/12** (incl. a negative control: skip the reset → chat is NOT fresh → reset → fresh, proving the check is live) + strict `tests/test_m14.py` **9/9**; regression: M2 **12/12**, M5 engine **18/18** + M5b runtime **10/10**, and the strict M8/M9/M10/M11 + bot_sim/tryme + builder + dashboard + isolation + auth-gate bundle **140 passed** (a separate M11 bundle re-run **59 passed**) — no regression. **No product source was edited to pass** (the three product files were already correctly implemented by the build session). Backend was restarted to load the new code (no auto-reload).
- ✅ **M15 (repo & file-organization cleanup — decision 0019)** — a clean, navigable monorepo with **ZERO behavior change**. The long files were split into focused, re-exporting sub-modules (`backend/app/services/booking.py`→`booking/` {settings,slots,crud,google}, `services/leads.py`→`leads/` {crud,query,funnel,_common}, `api/admin.py`→`api/admin/` {businesses,analytics,crm} composed by `__init__`; `gateway/src/index.js` split into `socket.js`/`webhook.js`/`routes.js` + a thin `index.js` bootstrap + `contract.js`/`config.js`/`logger.js`; `frontend/src/pages/LandingPage.tsx`→`components/landing/*`, `WhatsAppPage.tsx`→`components/whatsapp/*`) — every file now <500 lines and each `__init__`/index re-exports the FULL public surface so no import breaks. Tests reorganized into `backend/tests/{strict,narrated,isolation}` (move-only). **Naming approach (locked with Omer):** KEEP the technical folder names (`api`/`services`/`models`/`core`/`db` are universal conventions — renaming them breaks imports and confuses every dev/agent) and add **Hebrew "signs"** on top: a master map **`STRUCTURE.md`** (5 domains, every folder + key file explained in plain Hebrew), a `README.md` in each domain (`backend`/`gateway`/`frontend`/`infra`/`supabase`/`docs`), a one-line Hebrew header at the top of each code file, and **`ENV_SETUP.md`** (what to fill for real APIs — required/optional table + Fernet/token generators; the only EXTERNAL real-API keys are Gemini + Google OAuth, DB/Redis run real-locally in Docker). Refreshed the stale env templates (`infra/.env.example` + `.env.example` now list `GEMINI_API_KEY` — required now — and `ADMIN_EMAILS`) and the `CLAUDE.md` "Folder map" + root `README.md` (both now point to `STRUCTURE.md`/`ENV_SETUP.md`). **Documentation-only milestone; no product behavior changed** — the green baseline is the M14 **287-passing** strict bundle (run any `tests/test_*.bat` to re-prove). **NOTE:** a pre-existing **bot WIP** (first-message-greeting + auto-close, see commit `98e29ba`) that was in-flight was **SHELVED to a git stash** (NOT part of M15) so the M15 cleanup lands on a clean, green baseline.
- 🔧 **הצהרת נגישות + legal (2026-06-25, לא מקומיט עדיין)** — `frontend/src/pages/AccessibilityPage.tsx` שוכתב לעמוד **הצהרת נגישות** ציבורי עצמאי ל"בוטיק" לפי התקן הישראלי (תקנות שוויון זכויות לאנשים עם מוגבלות התשע״ג-2013 + ת״י 5568 / WCAG 2.0 רמה AA), במבנה דמוי-CARTAX, **מחובר לראוט הקיים `/accessibility`** (ללא שינוי routing) ו**בלי** עטיפת `LegalPage` (כדי להסיר את באנר ה"טיוטה" — זו הצהרה אמיתית). פרטי העסק יושבים כקבועי `BUSINESS` בראש הקובץ (טלפון `054-740-8309` מולא; **ח.פ. עדיין למלא**; דוא״ל `oyc3333@gmail.com`). eslint + tsc נקיים לקובץ. **במקביל (סשן אחר):** נוצר `docs/legal/terms-and-privacy-DRAFT.md` — טיוטת **תנאי שימוש + מדיניות פרטיות** v0.9. **שניהם עדיין לא מקומיטים** — קומיט ממוקד (לא `git add -A`) כשתהיו מוכנים, כדי לא לערבב בין הסשנים.
- ✅ **כפתור נגישות צף + תשתית i18n (2026-06-25, commit `6bb854a`, decision 0018)** — `AccessibilityWidget` תואם AA (5 אפשרויות: ניגודיות/טקסט גדול/קו תחתון/פוקוס/עצירת אנימציות) עם שמירה ב-localStorage, נטען פעם אחת ב-`App.tsx` בכל עמוד. הוקמה תשתית i18n (`i18next`+`react-i18next`, namespace `accessibility` he/en, שפה מ-`documentElement.lang`, fallback he) — המחרוזות הקיימות **לא** הוגרו ואין מתג שפה גלוי (scope מכוון). אומת ב-preview + tsc/eslint(jsx-a11y)/vite build ירוקים.
- ✅ **מודל תמחור: 3 מסלולים + מודולי add-on (2026-06-25, decision 0020)** — חינמי ₪0 / מקצועי ₪149 (מחיר רגיל ₪200) / עסקי ₪299 (₪349), **כולל מע"מ**, + שנתי "חודשיים חינם" (₪1,490 / ₪2,990). תקרות fair-use (מסלולי לידים 1/5/9, לידים 30/600/2000, פעולות AI 10/50/100, handoff 5-מספרים בחינם, booking רק בעסקי). פיצ'רים עתידיים = מודולי add-on נפרדים (RAG +₪199, תזכורות +₪49, דיוור +₪99). עמלת סוכן 15% חד-פעמית על המכירה הראשונה. **מיגרציה `0022_pricing_tiers_update.sql`** עדכנה את קטלוג ה-`plans` (חינמי/מקצועי/עסקי, `basic` הוסר, caps ב-`limits` jsonb) — הוחלה ואומתה (3 שורות). **לנדינג** עודכן (`landing/data.ts` + `PricingSection.tsx` + `styles.ts`): מחירי השקה מחוקים + שורת שנתי, **בלי פרסום "מענה אנושי"** בכרטיסי המחיר. **אכיפת התקרות במנוע עדיין לא מומשה** (קטלוג+תצוגה בלבד). Plan: [`decisions/0020-pricing-tiers.md`](decisions/0020-pricing-tiers.md).
- ✅ **לוגו "bootik" בכל העמודים + favicon (2026-06-25)** — החלפת הכיתוב הטקסטואלי "בוטיק" בלוגו התמונה בכל ה-brand spots: `OwnerHeader`, סרגל הצד של `DashboardLayout`, `LoginPage`, `LegalPage`, ו-nav+footer של דף הנחיתה (`landing/HeroSection` + `Footer`), וכן `favicon`/`apple-touch-icon` ב-`index.html`. **טיפול באסט:** הקובץ שסופק היה PNG עם רקע צרוב (RGB/לבן-על-שחור, בלי alpha אמיתי) — נוקה אוטומטית ב-Python (numpy/scipy): הסרת רקע (flood-fill מהשוליים + הסרה גלובלית מחוץ ל-bbox של הרובוט כדי לפתוח את חורי האותיות), חיתוך שוליים, ו**שתי גרסאות** ב-`frontend/public/`: `logo.png` (טקסט ירוק-כהה `#1b3a28` לרקעים בהירים) + `logo-on-dark.png` (לבן, לסרגל הדשבורד הכהה). גדלים: nav נחיתה 160px, פוטר 64px, headers 44px, סרגל 56px, התחברות 96px. הקבצים ~220KB (ירדו מ-944KB). tsc+eslint נקיים. **מקור הלוגו הגולמי (`logo.png` בשורש) לא מקומיט** — לא בשימוש האפליקציה. (טקסט "בוטיק" בגוף משפטי/צ'אט/זכויות-יוצרים הושאר ככתב במכוון.)
- ✅ **מודל סטטוסים אחיד לשיחות/לידים + badge לא-נקראו (2026-06-26, decision 0021)** — איחוד ה-state machine (bot / waiting+human / closed) + שלוש סיבות סגירה מפורשות בעמודה חדשה `leads.close_reason` (`completed`/`abandoned`/`answered`). **באג מרכזי שתוקן:** שעון ה-60 דק' לנטישה התאפס רק במצב בוט — עכשיו מתאפס בכל הודעה נכנסת גם ב-`waiting`/`human` (לקוח שמשוחח עם נציג לא יסומן בטעות "ננטש"). **מיגרציה `0023_close_reason.sql`**: עמודה + `sweep_abandoned_leads` מחזיר עכשיו `TABLE(lead_id, conversation_id)`, חותם `abandoned`, וה-sweep סוגר גם את שיחת ה-Redis. + תיקון replay ב-`0006` (DROP FUNCTION לפני CREATE כי ה-migrate ללא ledger). **unread badge** (ירוק-וואטסאפ, מספר לבן) על טאב "שיחות": מונה Redis per-conversation (+1 בכל הודעה נכנסת, אפס בפתיחת השיחה), `unread_total` ב-`GET /api/conversations`. תוויות עברית: "ליד הושלם"/"ליד ננטש"/"מענה הושלם". **QA:** 23 strict + 20 narrated + 61 regression (M2/M10/M14) ירוקים. **אכיפת התקרות לא נכללת** (נפרד). Plan: [`decisions/0021-conversation-lead-status-model.md`](decisions/0021-conversation-lead-status-model.md). ⚠️ **נמצאו ~12 בדיקות ישנות שנכשלות כבר על main** (M8/M9/bot_sim/dashboard/lead_status) בגלל commit `0dd58ad` (first-message greeting) — לא רגרסיה מהמיילסטון; צריך לעדכן את הבדיקות הישנות.
- ✅ **תיקון gateway: getSock circular-dependency + re-pair (2026-06-26)** — הבוט הפסיק לענות: הודעות נכנסו ל-backend (200, replyCount:1) אבל ה-gateway קרס בשליחת התשובה על `socketModule.getSock is not a function`. השורש: תלות מעגלית `socket.js`↔`webhook.js` שנוצרה בפיצול M15 — `socket.js` עשה `module.exports = {…}` (אובייקט חדש) אחרי ש-`webhook.js` כבר תפס את ההפניה הריקה. **תוקן** ב-`gateway/src/socket.js` עם `Object.assign(module.exports, {…})` (שומר את אותה הפניה). בנוסף, ה-session של Baileys השתבש (Bad MAC) → אופס דרך **סריקת QR מחדש מעמוד `/whatsapp`** (ה-creds הישנים גובו ל-`gateway/auth_backup_pre_repair/`, gitignored). אומת חי: הודעה נכנסת → הבוט עונה, וגם הלקוח מקבל. (שגיאת `/send-bot jidDecode` הייתה רק מה-session הישן — לא חוזרת אחרי ה-re-pair.)
- ✅ **מחיקת ליד + סימון נקרא + סדר טאבים (2026-07-15, decision 0022)** — (1) כפתור "מחק" בכל כרטיס ליד (עמוד לידים): מחיקה לצמיתות מה-DB עם Modal אישור. (2) כפתור X לסימון התראה בודדת כנקראה + "סמן הכל כנקרא" בעמוד בית — שרידותי לרענון (עמודה `leads.feed_seen_at`). (3) הטאב "הכול" הועבר למקום האחרון גם בלידים וגם בשיחות. Migration `0024_lead_feed_seen.sql`. Plan: [`decisions/0022-lead-delete-mark-read.md`](decisions/0022-lead-delete-mark-read.md).
- ✅ **M6b (WhatsApp רב-דיירי — decision 0023)** — 2026-07-16, commits `ac28651` + `6a4b674` + `24841be`. **סוקט Baileys אחד לכל עסק**: `gateway/src/manager.js` מסנכרן סשנים מול ה-backend כל 15 שניות, `session.js` מנהל את מחזור החיים (self-chat, reconnect עם backoff, שליחות מסודרות, logout→relink), והספייק החד-דיירי `socket.js` נמחק. **ה-creds כבר לא על הדיסק**: `dbAuthState.js` שומר את ה-auth state כ**מעטפה מוצפנת אחת** ב-`whatsapp_credentials` דרך ה-API הפנימי `/internal/wa/*` (`X-Gateway-Token`) — **ה-KEK לעולם לא יוצא מה-backend**, והטבלה נגישה רק דרך pool של `gateway_role` (ל-`app_role` אפס grant). **QR per-business** ב-Redis (`wa:qr:{business_id}`), ו-`gateway_account_id = business_id` (החליף את `'spike'`). **מספר אחד = עסק אחד:** `phone_hmac` + `wa_phone_conflict()` (migration `0027`) — עסק שני שסורק מספר מקושר נדחה, מנותק, ומקבל שגיאת `phone_conflict`. **חוסן:** Bad-MAC auto-recovery (ניקוי ה-Signal session של אותו איש קשר בלבד) + getMessage resend cache. דלתות ה-dev של השער (`/qr`,`/inbox`,`/send`,`/info`) **הוסרו**. Migrations `0026`+`0027`. **Verify:** `backend/tests/strict/test_m6b_wall.py` — **12 בדיקות, כל שש תכונות ה-WALL ירוקות**. **אומת חי:** שני עסקים במקביל עם creds נפרדים; הסשן שרד restart של השער בלי סריקה מחדש. Plan: [`decisions/0023-m6b-multitenant-whatsapp.md`](decisions/0023-m6b-multitenant-whatsapp.md).
- ✅ **הקשחת אבטחה + שער QA (decision 0024)** — 2026-07-16, commit `037a924` (PR #1). קובץ סודות **אחד** `infra/.env` (שונה מ-`.env.local`) + template אחד במעקב; **תיקון דליפת PII ב-access log** (`app/core/request_log.py` מחליף את `uvicorn.access` — שורה מצונזרת בלי query string, אחרי שדלפו `code`/`state` של OAuth ו-`cancel_token`); **רשת פרודקשן** (`docker-compose.prod.yml` + `Caddyfile` = הדלת הציבורית היחידה); `/docs`+`/redoc`+`/openapi.json` **כבויים** כש-`APP_ENV != dev`; שלושה guards (secret/log-PII/port-exposure) + `test_e2e_auth_matrix.py` (12); collection של **Postman/newman** ב-`tests/postman` (18/18). פירוט מלא: [`security/hardening-report.md`](security/hardening-report.md) + [`security/production-networking.md`](security/production-networking.md). Plan: [`decisions/0024-security-hardening-and-qa-gate.md`](decisions/0024-security-hardening-and-qa-gate.md).
- ✅ **עלייה לאוויר (decision 0025)** — 2026-07-17, commits `8f37108`/`9755991`/`4fcf3f9`/`8ce0a0f`/`f55fd46`. Lightsail פרנקפורט + static IP + DuckDNS + Caddy HTTPS אוטומטי + firewall 22/80/443 + מסך הסכמה של Google **מפורסם** + `/root/deploy.sh` + GitHub Action (כרגע נכשל על ה-SSH key). ארבעה תיקוני deploy נלמדו: bind-mount של השער ב-prod, `COPY public/` ב-`Dockerfile.prod`, `PUBLIC_DOMAIN` ריק שובר את ה-Caddyfile, ו-timeouts של 120s ל-AI. Runbook: [`DEPLOY.md`](DEPLOY.md) · Plan: [`decisions/0025-production-deployment.md`](decisions/0025-production-deployment.md).
- ✅ **מיתוג: אייקון ה-B-robot (decision 0026)** — 2026-07-17, commits `e940443`/`d69b20c`/`0c2e84b`. `botik-icon.png` אחד (סימן B-robot עם קו מתאר ירוק) בכל נקודות המותג כולל favicon/apple-touch-icon; חותמת **"דמו"** מסובבת על כותרת המסלולים + "המחירים להמחשה בלבד"; brand intro ממורכז וצף בראש דף הנחיתה (1.9s, מודע ל-reduced-motion). Plan: [`decisions/0026-botik-brand-icon.md`](decisions/0026-botik-brand-icon.md).
- ✅ **M17 (חיפוש בלידים + ייצוא לאקסל)** — 2026-07-20, commit `74d6bcc`, חי בפרודקשן. חיפוש חופשי בטבלת הלידים (שם/טלפון/מסלול/**כל מפתח שאלה**/**כל ערך תשובה**/**שמות קבצים**) — רץ בדפדפן כי `answers` מוצפן כבלוב יחיד ו-SQL לא יכול להתאים עליו. ייצוא `GET /api/leads/export` ל-`.xlsx` עם **טאב לכל מסלול** (עמודות התשובות מחושבות לכל גיליון בנפרד), צבעי טאב מעומעמים, RTL מלא (`readingOrder=2` ברמת העמודה — `sheet_view.rightToLeft` לבדו הפך רק עמודות ולא טקסט), הגנת formula-injection, וקבצים כקישורים למסלול המוגן-סשן. תקרה `EXPORT_MAX_ROWS=50_000` (נמדד: 3,000 לידים → 3.5 שניות, שיא 4.1MB, streaming). **11/11 strict.** ⚠️ כלל ההתאמה קיים **פעמיים** (`leads/search.py` + `dashboard/leadSearch.ts`) — חייבים להישאר זהים, אחרת הקובץ יפסיק להתאים למסך. Plan: [`decisions/0027-m17-leads-search-and-export.md`](decisions/0027-m17-leads-search-and-export.md).
- ⬜ **M6 (rest) — next:** per the checklist (QR onboarding, encrypted cred persistence, hardened inbound, reconnection — outbound send is now wired via M6a.2 `/send-bot`). **NOTE:** M6b must handle WhatsApp **LID addressing** for real customers (see the LID fix above) — phone-based routing won't work without LID→phone resolution.

## 🔮 Future / backlog (decided, NOT scheduled)

- ⬜ **מדיניות שמירה לקבצים שלקוחות מעלים (R2) — אין כזו היום.**
  **נמצא 2026-07-21 תוך כדי M19.** `file_storage.delete_object` קיים, אבל שני
  הקוראים היחידים שלו ב-`api/internal_media.py` הם **גלגול לאחור** כשההעלאה
  נכשלת. אחרי שמירה מוצלחת — **הקובץ נשאר ב-R2 לנצח.**

  **הסתירה שנוצרה ב-M19:** השיחות נשמרות 72 שעות, הקבצים שצורפו אליהן — לתמיד.
  הודעה שאמרה "צירפתי את הצילומים" תיעלם, והצילומים יישארו.

  **למה זה חשוב, ולמה זה לא עניין של כסף:** בקצב של 40 תמונות ליום לבוט מדובר
  ב-‏~600MB לחודש לבוט, שזה דולרים בודדים ב-R2. הבעיה היא **מה** שנשמר: צילומי
  נזק, מסמכי זיהוי וקבלות — PII של לקוחות קצה שמעולם לא נרשמו למערכת שלנו,
  מוחזק ללא הגבלת זמן וללא מסלול מחיקה.

  **מה יידרש:** החלטה על חלון שמירה (למשל 30/90 יום), משימת רקע שמוחקת גם את
  האובייקט ב-R2 וגם את שורת `lead_files`, ומחיקה מדורגת כשליד נמחק. שים לב
  ש-`storage_key` כבר מתחיל ב-`{business_id}/`, מה שמאפשר גם lifecycle rule
  ברמת הדלי או מחיקה בכמות — התשתית לכך קיימת.

- ⬜ **"מודל AI" — בוט שגם עונה, לא רק אוסף (מסלול תמחור נפרד).**
  **החלטת Omer, 2026-07-20: המוצר הנוכחי נשאר מודל האיסוף. זה יהיה מוצר בתמחור אחר.**

  **המצב היום (למה זו בכלל שאלה):** מנוע השיחה `bot_engine.py` הוא מכונת מצבים
  **דטרמיניסטית וטהורה** — `phase` (new/menu/in_flow/handed_off) + `step_index`.
  "מה לענות" = "מה השאלה הבאה ברשימה". בחירת מסלול = מספר או התאמת מחרוזת לתווית
  (`_pick_flow`). **אין קריאת LLM בנתיב ההודעה הנכנסת** — ה-AI פועל רק בבונה הבוטים
  ובברכת ההזמנות, כלומר במסכי הבעלים, לא בשיחת הלקוח.

  **מה זה קונה לנו היום, ומה ייפרץ אם נשנה:**
  1. **אבטחה** — אין LLM ואין גישה לנתונים בנתיב הזה, ולכן **אין prompt-injection ואין
     נתיב exfiltration**. לקוח לא יכול להזריק הוראות: אין למי. מודל AI בשיחה **פותח
     את שטח התקיפה הזה מאפס** ויחייב בידוד הקשר קפדני.
  2. **עלות** — הודעה = חיפוש בטבלה. בגלל זה השרת בולע מאות בוטים מבחינת עומס
     (ראה [`SERVER.md`](SERVER.md) §5). AI לכל הודעה הופך את זה לעלות משתנה לכל הודעה —
     **בדיוק הסיבה שזה חייב מסלול תמחור נפרד.**
  3. **דטרמיניזם** — אותו קלט → אותה תשובה, תמיד, וניתן לבדיקה. תשובות LLM אינן.

  **המחיר של המצב הנוכחי (המגבלה שדוחפת לשינוי):** הבוט **לא יכול לענות על שאלה
  חופשית**. "כמה עולה ביטוח חיים לגבר בן 40?" מקבל דחיפה חזרה לתפריט. השסתום היחיד
  הוא `handoff_keywords` → אדם, וזו העברה ולא מענה.

  **קשור ל-:** [`decisions/0020-pricing-tiers.md`](decisions/0020-pricing-tiers.md) — הפיצ'רים
  העתידיים כבר מוגדרים שם כמודולי add-on נפרדים (RAG ‎+₪199 הוא ככל הנראה הבית הטבעי
  לזה). דורש גם את אכיפת התקרות, שעדיין לא מומשה.

## What works RIGHT NOW
- Full local stack via `run.bat` (or `make dev`): **backend `:8000` · gateway `:3000` · frontend `:5173` · postgres · redis**, health-gated startup.
- **WhatsApp gateway is connected** (Baileys). Real inbound messages → gateway → backend `/webhook/whatsapp` (200, **redacted** log — no phone/text).
- Backend `/healthz` confirms Postgres + Redis reachable. Webhook auth (`X-Gateway-Token`) enforced (401 on bad token).
- Frontend: RTL Hebrew page + a live "stack health" panel.

## How to run & test
- **Start:** double-click `run.bat` (Docker Desktop must be running). **Stop:** `stop.bat`.
- **URLs** (use `127.0.0.1` if `localhost` is flaky on Windows):
  - Frontend: `http://127.0.0.1:5173`
  - Connect WhatsApp (QR): `http://127.0.0.1:3000/qr`
  - 📥 Dev inbox (see received message content): `http://127.0.0.1:3000/inbox`
  - 📤 Dev send (send a message): `http://127.0.0.1:3000/send`
- **Secrets:** `infra/.env` (generated, git-ignored). If missing, `run.bat` regenerates it.

## What's built (the code so far)
- `backend/` — FastAPI: fail-closed config, `/healthz`, `/webhook/whatsapp` (+ redacted log).
- `gateway/` — Node/Baileys: QR connect, `/healthz`, forwards inbound; **+ dev-only `/inbox` & `/send`**.
- `frontend/` — Vite + React + Tailwind + RTL: hero + StackHealth.
- `infra/` — `docker-compose.yml` (health-gated), `.env.example`; plus root `Makefile`, `run.bat`, `stop.bat`.
- `supabase/` — **real schema** `migrations/0001…0004` (roles+pgcrypto, RLS bridge, 9 tables, RLS+grants) + `seed.sql` (2 demo tenants). `0000_init.sql` stays an empty marker.
- `backend/app/` — **M2 additions:** `core/crypto.py` (dual-key, fail-loud), `db/session.py` (tenant `SET LOCAL`), `services/live_chat.py` (Redis cache isolation); `core/config.py` now requires the encryption keys (fail-closed).
- `backend/tests/` — `isolation/` suite (10 passing) + `demo_isolation.py` (the readable 9/9 story) + `test_secret_guard.py`; **M3:** `m3_full_test.py` (the 5/5 story) + `test_auth_gate.py` (strict gate).
- `backend/app/` — **M3 additions:** `services/auth.py` (Google OAuth + opaque Redis sessions), `core/deps.py` (`current_session/current_user/current_business`), `api/auth.py` (`/auth/google|callback|logout`), `api/me.py` (gated `/api/*` group + `GET /api/me`), `models/auth.py`; `core/config.py` now also requires `GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI` + `SESSION_SECRET` (fail-closed). `supabase/migrations/0005_auth_bootstrap.sql` adds the `provision_owner` / `get_user_businesses` SECURITY DEFINER funcs.

## Fixes we already made today (don't re-hit these)
- **Port 6379 clash** → postgres/redis are **not** published to the host (internal-only on the Docker network).
- **Redis "unhealthy"** → added `REDISCLI_AUTH` env so the healthcheck can authenticate.
- **Frontend crash (`@rollup/rollup-linux-x64-gnu`)** → Windows `node_modules` leaked via the bind mount; fixed with an **anonymous volume** for `/app/node_modules` + the frontend Dockerfile uses `npm install`.
- **Gateway restart** reconnects automatically from saved creds in `gateway/auth/` (no QR re-scan).

## DEV-ONLY — must be cleaned up before production (tracked for M6)
- `gateway` `/qr`, `/inbox`, `/send` are **unauthenticated dev tools that expose content** — remove/secure them.
- Baileys creds sit as files in `gateway/auth/` (spike) — must move to the **encrypted DB** (crown jewel, `whatsapp_credentials`).

## Next steps
1. *(recommended)* a **git checkpoint** of the M3 working state.
2. **One-time manual check:** click "Sign in with Google" at `http://localhost:5173`, confirm you land logged-in on your own business, then log out (OAuth needs a real browser, so a script can't do this part).
3. Continue M4→M9 per [`spec/mvp-checklist.md`](spec/mvp-checklist.md).

## How to verify M3 (anytime)
- Double-click `tests/test_m3.bat`, or by hand:
  - M3 story: `docker compose --env-file infra/.env -f infra/docker-compose.yml run --rm backend sh -c "cd /app && PYTHONPATH=/app python tests/m3_full_test.py"` → **5/5**.
  - M3 gate: same wrapper, `pip install -q pytest pytest-asyncio && PYTHONPATH=/app python -m pytest tests/test_auth_gate.py -q`.
  - No-regression: re-run `tests/m2_full_test.py` → still **12/12**.

## How to verify M2 (anytime)
- `make demo-isolation` → reads the 9/9 "wall holds" story (start here).
- `make isolation` → the pytest gate (10 passing), connecting as the real non-service roles.
- `make demo-break` → drops a `WITH CHECK`, shows the demo catch it (8/9), then restores.
- Note: in plain Git Bash `make` may be absent; the equivalent `docker compose … run` commands are in the `Makefile`.

## The map of everything
- **Plan:** [`spec/roadmap.md`](spec/roadmap.md) · [`spec/mvp-checklist.md`](spec/mvp-checklist.md) · [`spec/build-guide.md`](spec/build-guide.md) · [`spec/architecture.md`](spec/architecture.md) · [`spec/data-model.md`](spec/data-model.md)
- **Decisions:** [`decisions/`](decisions/) (0001 Baileys/QR · 0002 multi-tenant · 0003 model · 0004 MVP scope · 0005 auth/data · 0006 Redis live-chat · … · 0023 M6b multi-tenant WhatsApp · 0024 security hardening · 0025 production deployment · 0026 Botik brand icon)
- **Production:** [`DEPLOY.md`](DEPLOY.md) (runbook) · [`security/production-networking.md`](security/production-networking.md) · [`security/hardening-report.md`](security/hardening-report.md)
- **Old-system map:** [`system-map/`](system-map/), [`bugs.md`](bugs.md), [`security-issues.md`](security-issues.md)
- **AI tooling:** `.claude/agents/` (scanners + `data-architect` + `devops_aws`), `.claude/skills/progress_report`, `.claude/workflows/`
